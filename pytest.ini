[pytest]
testpaths = tests
norecursedirs = data legacy_archive .git __pycache__ .pytest_cache pytest-cache-files-*
pythonpath = .
markers =
    docker_integration: requires Docker and the pinned Astra Stage 2C runtime image
    local_model_integration: requires explicitly enabled locally cached learned models
