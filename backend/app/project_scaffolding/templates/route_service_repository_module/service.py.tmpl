from __future__ import annotations


class ResourceService:
    """Deterministic scaffold for the ${resource_name} resource service."""

    def __init__(self, repository: object) -> None:
        self._repository = repository
